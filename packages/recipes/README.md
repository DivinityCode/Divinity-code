# Recipes Package
Owner: Builder Experience

Defines guided starter recipes for onboarding and demo flows.

## Current Surface
- `STARTER_RECIPES` contains the built-in guided recipes.
- `publicStarterRecipes()` returns recipe records for CLI output.
