# Memory Package
Owner: Intelligence Services

Creates provenance-backed memory entries scoped to session, project, and team contexts.

## Current Surface
- `createRunMemoryEntries(...)` derives session/project/team memory entries from run context, with source provenance, timestamps, confidence, and stable IDs.
