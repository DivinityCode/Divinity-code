# Connectors Package
Owner: Platform

Defines connector adapter metadata for external context sources that future runners and dashboards can attach to runs.

## Current Surface
- `CONNECTOR_ADAPTERS` lists ticket, docs, and CI status reference adapters.
- `publicConnectorAdapters(...)` returns defensive copies for capability discovery.
