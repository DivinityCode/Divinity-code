# API App
Owner: Control Plane

Planned endpoints: task creation, run retrieval, preflight checks.

## Current Endpoints
- `GET /health`
- `POST /preflight`
- `POST /tasks`
- `GET /approvals`
- `GET /runs/:id`
- `GET /runs/:id/events`
- `POST /runs/:id/approval`
